@page "/user/logout"
@using MindKey.Client.Services
@inject IUserService UserService
@inject EventService eventService

@code {
    protected override async void OnInitialized()
    {
        await UserService.Logout();
        eventService.ChangeLoginStatus(false);

    }
}